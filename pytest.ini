[pytest]
# Ensure both import styles work:
# - "from app..." by adding backend/ to PYTHONPATH
# - "from backend..." by keeping repo root on PYTHONPATH
pythonpath =
    .
    backend

# Limit test discovery to backend/tests
testpaths =
    backend/tests

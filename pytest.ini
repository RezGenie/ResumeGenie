[pytest]
# Ensure the backend package is importable as top-level (so `app.*` works)
pythonpath =
    backend

# Limit test discovery to the backend tests directory
testpaths =
    backend/tests

# Pytest defaults
addopts = -ra
asyncio_mode = auto

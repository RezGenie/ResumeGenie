[pytest]
# Ensure the backend directory is on the Python path when running inside the backend container
pythonpath = .

# Discover tests only in the local tests folder
testpaths = tests

# Pytest defaults
addopts = -ra
asyncio_mode = auto
